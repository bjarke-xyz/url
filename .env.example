# All optional; the defaults below are what the server uses when unset.
PORT='3000'
DATABASE_PATH='./url.db'

# Prefix for generated short links. When unset, derived from the request's
# Host header and X-Forwarded-Proto.
# BASE_URL='https://example.org'
